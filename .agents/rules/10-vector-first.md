# Vector-First Production Rule

For logo marks, wordmarks, icons, patterns, diagrams, seals, and graphic motifs:
- prefer SVG as canonical source
- keep paths editable
- avoid embedding raster images inside master SVGs
- preserve consistent viewBox and naming
- export raster derivatives only from the vector master
