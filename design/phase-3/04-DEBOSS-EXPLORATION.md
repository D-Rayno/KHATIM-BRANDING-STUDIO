# 04 — DEBOSS EXPLORATION (THE MATRIX AND THE IMPRESSION)

> **Label: EXPLORATION — NOT FINAL**
> The deboss metaphor tested across at least four interpretations, per the Phase 3 brief §6. Synthesized from Agents 01, 02, 03, 05, 07, 09.

## The four interpretation tests

### 1. Physical-inspired (tactile deboss with real light)
- Test: displaced geometry rendered with physical light/shadow.
- Verdict: **Viable only with a rendering-discipline spec** (Agent 01 §10.2: light angle, shadow falloff, scale thresholds). Agent 09's skeuomorphism attack stands as a YES-block until the spec passes. Agent 03 rejects wax/literal stamp outright.
- Survivor: F-02's die/impression pair as physics, provided geometry-first rendering.

### 2. Flat geometric (displacement as pure geometry; no tone)
- Test: the press shown as step/perforation in silhouette — no gradient, no shadow.
- Verdict: **Strongest honest reading.** F-16's physics is instructive but dies as a mark; F-01's pressed cell held as flat geometry passes (per F-12's rejection — folds needing tone fail).
- Survivor: F-01 re-constructed flat at 16px (Agent 07 spec).

### 3. Negative-space (the impression as absence)
- Test: the deboss reduced to the pure void.
- Verdict: **Cleanest and most durable reduction** (Agent 02); clean TRUE whitespace (Agent 08); 16px benchmark (Agent 07). Semiotionally mute on causation — motion/system must narrate "pressure" (Agent 03).
- Survivor: F-05 (Finalist B).

### 4. Abstract relational (matrix ↔ impression as paired relationship)
- Test: two related states — solid die / void impression — encoding asymmetric cryptography (private acts → public proof).
- Verdict: **Most semiotically complete** (Agent 03) at a two-object identifiability cost. Best as system behavior across applications (Agent 05) rather than a single mark.
- Survivor: F-02 (retained as behavior).

## Required test questions — answered

- **"Impression" without showing a stamp?** YES — via displacement event (F-01) and void (F-05); stamp depiction rejected (F-02's risk managed; literal wax banned).
- **"Proof" without a checkmark?** YES — state-change as proof visual (Agent 01 §8); checkmarks banned in the anti-list.
- **"Authority" without a shield?** YES — permanence physics + measurement language + triadic composition (Agent 01 §6).
- **"Heritage" without decorative ornament?** YES — nuqta as structural metrology only (Agents 03, 05); visible pattern = kill.

## Gate verdict for the deboss

The metaphor **survives** — but only through interpretations 2–4 (flat geometric, negative-space, relational). Interpretation 1 (physical-inspired) is admitted only under the rendering-discipline spec. The literal-stamp reading is rejected per the gate (strategy/14 §1).

*Deboss exploration remains EXPLORATION; no metaphor locked.*
